import { useEffect, useState, useRef } from 'react';

interface ChatbotWidgetProps {
  userId: string;
}

const ChatbotWidget = ({ userId }: ChatbotWidgetProps) => {
  // Voiceflow widget is fully removed. Placeholder for future chatbot integration.
  return null;
};

export default ChatbotWidget;